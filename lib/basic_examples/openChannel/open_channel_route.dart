import 'package:flutter/material.dart';

class OpenChannelRoute extends StatefulWidget {
  const OpenChannelRoute({Key? key}) : super(key: key);

  @override
  OpenChannelRouteState createState() => OpenChannelRouteState();
}

class OpenChannelRouteState extends State<OpenChannelRoute> {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    throw UnimplementedError();
  }
}
